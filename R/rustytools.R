#' Get consensus
#' @description It will take a string and print the coordinates of every repeated character substring made up of either "ACGT".
#' @export
#'

get_consensus<-function(str){
  getconsensus(str)
}
